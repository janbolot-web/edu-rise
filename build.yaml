targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
      freezed:
        generate_for:
          include:
            - lib/features/*/domain/entities/**.dart
            - lib/features/*/data/models/**.dart
